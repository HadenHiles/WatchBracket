'use client';
import { useState } from 'react';
import { useRouter } from 'next/navigation';
import { BrandLogo } from '../../components/brand-logo';

export default function DisplayPairPage(){const router=useRouter();const[code,setCode]=useState('');const[error,setError]=useState('');const[loading,setLoading]=useState(false);async function submit(e:React.FormEvent){e.preventDefault();setLoading(true);setError('');try{const response=await fetch('/api/displays/pair',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({pairingCode:code,displayName:'Living room display'})});const body=await response.json();if(!response.ok)throw new Error(body.message??'Could not pair display');router.replace(`/display/${body.displaySessionId}`);}catch(e){setError(e instanceof Error?e.message:'Could not pair display');setLoading(false)}}return <main className="shell"><div className="hero"><BrandLogo label="Shared display"/><h1>Shared display</h1><p className="lead">Generate a display code from the host's room controls, then enter it here.</p></div><form className="card stack" onSubmit={submit}>{error&&<p className="error">{error}</p>}<label>Pairing code<input autoFocus inputMode="numeric" maxLength={6} value={code} onChange={(e)=>setCode(e.target.value.replace(/\D/g,''))}/></label><button disabled={loading}>{loading?'Pairing…':'Connect display'}</button><a href="/display/test">Open Presentation Test Mode</a></form></main>}
