# Milestone 2 Cast integration point

The current `/cast/receiver/` build is intentionally a static readiness shell. Milestone 2 adds Google Cast registration, supported-browser sender loading, a custom namespace, single-use launch-token issuance and exchange, direct WebSocket-only receiver connectivity, lifecycle/idle handling, and physical-device acceptance testing. No Cast SDK or token is present in Milestones 0–1.

